{
  "name": "resolve",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "web": "pnpm --filter web dev",
    "api": "pnpm --filter api dev",
    "extension": "pnpm --filter extension build",
    "preview": "pnpm --filter web preview",
    "install:all": "pnpm install -r"
  },
  "devDependencies": {
    "turbo": "^2.10.7"
  },
  "dependencies": {
    "zod": "^4.4.3"
  }
}